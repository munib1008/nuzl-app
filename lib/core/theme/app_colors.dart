import 'package:flutter/material.dart';

/// NUZL design-system colors — premium teal token set (§ design foundation).
class AppColors {
  // Brand
  static const primary = Color(0xFF0F766E);
  static const primaryHover = Color(0xFF115E59);
  static const primaryDark = Color(0xFF042F2E);
  static const primaryTint = Color(0xFFCCFBF1); // primaryLight
  static const primaryBright = Color(0xFF14B8A6); // accent
  static const secondary = Color(0xFF042F2E);
  static const accentGold = Color(0xFFF59E0B); // amber highlight
  static const accentGoldTint = Color(0xFFFEF3C7);
  static const accentCream = Color(0xFFE6D1B4);
  static const accentPlum = Color(0xFF533946);

  // Semantic
  static const success = Color(0xFF10B981);
  static const warning = Color(0xFFF59E0B);
  static const danger = Color(0xFFEF4444);
  static const info = Color(0xFF14B8A6);

  // Light neutrals
  static const bg = Color(0xFFF8FAFC);
  static const surface = Color(0xFFFFFFFF);
  static const surface2 = Color(0xFFF1F5F9);
  static const border = Color(0xFFE2E8F0);
  static const borderStrong = Color(0xFFCBD5E1);
  static const text = Color(0xFF0F172A);
  static const textMuted = Color(0xFF64748B);
  static const textSubtle = Color(0xFF94A3B8);

  // Dark neutrals (NOT inverted; never pure black)
  static const dBg = Color(0xFF07141A);
  static const dSurface = Color(0xFF0B1F28);
  static const dSurface2 = Color(0xFF102733); // card
  static const dBorder = Color(0xFF1E3A46);
  static const dBorderStrong = Color(0xFF2A4D5A);
  static const dText = Color(0xFFF8FAFC);
  static const dTextMuted = Color(0xFF94A3B8);
  static const dTextSubtle = Color(0xFF64748B);
  static const dPrimary = Color(0xFF2DD4BF);
  static const dPrimaryTint = Color(0xFF0E2E2A);
  static const dGold = Color(0xFFF59E0B);
}
