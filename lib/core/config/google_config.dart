/// Google OAuth web client ID. Required for Google sign-in on Flutter web.
/// Get it from Google Cloud Console → Credentials → OAuth 2.0 Client (Web).
/// Mobile uses the platform config files (google-services.json / plist) instead.
const String googleWebClientId = ''; // e.g. '1234-abc.apps.googleusercontent.com'
